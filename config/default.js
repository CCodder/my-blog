const config = {
    // 启动端口
    port: 8000,
    // 数据库配置
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: '12345678',
        PORT: '3306',
        HOST: 'localhost',
        characterEncoding: 'utf-8',
    },
}

module.exports = config